# Suggested keywords

**Do not use this file as the ranking.** One ranked table of participant queries (spaces, never `keyword - keyword` or hyphen-joined labels) lives in **`RANKED_KEYWORDS.md`**. No invented volumes. Focus leftovers: `.ca` `.us` `.co.uk`/`.uk` (no `iptv` in UK labels) `.dk` `.no` `.fi` — not `.fr`. `.se` UNKNOWN is not a buy.

Noxtools this UTC hour (02:23 probe): skip login POST (Cloudflare 403 / session expired). Not HTTP 429. Never free Semrush.
