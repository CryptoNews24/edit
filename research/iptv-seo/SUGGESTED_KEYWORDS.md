# Suggested keywords

**Do not use this file as the ranking.** One ranked table of participant queries (spaces, never `keyword - keyword`) lives in **`RANKED_KEYWORDS.md`**. Hyphen-joined queries only if verified high volume and the hyphen leftover is AVAILABLE. No invented volumes. Focus leftovers: `.ca` `.us` `.co.uk`/`.uk` (no `iptv` in UK labels) `.dk` `.no` `.fi` — not `.fr`. `.se` UNKNOWN is not a buy.

Noxtools this UTC hour (05:xx): skip login POST after Cloudflare 403 (Just a moment). Not HTTP 429. Never free Semrush. Ranked table only: RANKED_KEYWORDS.md.
